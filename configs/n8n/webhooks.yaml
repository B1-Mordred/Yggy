version: 1

webhooks:
  - id: daily_briefing_stub
    name: Daily briefing webhook stub
    path: /webhook/yggy/daily-briefing
    method: POST
    enabled: true
    max_payload_keys: 10
    description: Internal-only stub for testing approved automation dispatch to n8n.
